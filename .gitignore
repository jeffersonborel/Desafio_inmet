# Ignorar a pasta node_modules (dependências do npm/yarn)
node_modules/

# Ignorar logs e arquivos de depuração
npm-debug.log
yarn-debug.log
yarn-error.log
.log

# Ignorar arquivos de ambiente (se usar .env)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Ignorar configurações do editor (opcional, mas comum)
.vscode/

# Adicione aqui qualquer outro arquivo que você saiba que pode conter segredos
# ou que não deve ser versionado (ex: chaves, certificados, etc.)
# Exemplo:
# *.key
# *.pem
# secrets.json